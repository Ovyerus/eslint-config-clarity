/* eslint-disable */
// Used to allow us to not need to provide our dependencies as only peers, which
// can be annoying for end-users. Users instead require this file
// (eslint-config-clarity/patch) to have ESLint resolve the dependencies
// relative to our config, which then fixes it.
require("@rushstack/eslint-patch/modern-module-resolution");
